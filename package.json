{
  "name": "ondashbackend",
  "version": "1.0.0",
  "description": "Backend for dash with WS implementation consume arduino data.",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "serialport": "^9.2.3",
    "socket.io": "^4.2.0"
  }
}
